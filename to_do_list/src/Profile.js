export class Profile {
  constructor (id, username, email, password, isLogged) {
    this.id = id
    this.username = username
    this.email = email
    this.password = password
    this.isLogged = isLogged
  }
}
